<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="main.css">
    <title></title>
  </head>
  <body>
    {{ #users }}
    <div class="users">
      <img src={{avatar}} alt="robot avatars"/>
      <h1>{{ name }}</h1>
      {{ #job }}
      <h2>{{ job }}, {{company}}</h2>
      {{ /job }}
      {{ ^job }}
      <h2 style="color:red">Available for Hire</h2>
      {{ /job }}
    </div>
    {{ /users }}
  </body>
</html>
